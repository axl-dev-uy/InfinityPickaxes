# ==============================================================================
#                  INFINITY GEAR - MAIN CONFIGURATION
# ==============================================================================

# Configuration Version - do not change manually
config-version: "2.0.0"

# Plugin language (locales/en.yml or locales/es.yml)
# Default: "en" (English). Set to "es" for Spanish.
language: "en"

# Forensic mining alerts; reloaded through the existing configuration/admin reload.
# Incidents remain in MariaDB even with alerts disabled or no staff online.
# No player-facing notices, retries or compensation are performed.
mining-incidents:
  alerts:
    enabled: false
    permission: "noxwardarchives.alerts.mining"
    interval-seconds: 60
    message: "[Mining] {count} voided operation(s). Latest: {operation}. Inspect the mining incident ledger."

# General settings
settings:
  # Base pickaxe item material (e.g. NETHERITE_PICKAXE, DIAMOND_PICKAXE)
  default-material: NETHERITE_PICKAXE
  # Vanilla Efficiency applied to newly created or explicitly converted pickaxes.
  # Efficiency is free; managed Fortune and Silk Touch each consume a socket.
  default-efficiency-level: 20
  # Automatically convert any regular vanilla pickaxe held or used by a player into an Infinity Pickaxe
  auto-convert-vanilla: false
  # All pickaxes are unbreakable by default
  unbreakable: true
  # Hide vanilla attributes/unbreakable tags on item
  hide-flags: true
  # Max pickaxe level
  max-level: 100

# Trigger to open the Pickaxe Menu
interaction:
  # Action to open GUI: SHIFT_RIGHT_CLICK, SHIFT_LEFT_CLICK, or BOTH
  trigger: SHIFT_RIGHT_CLICK
  # Allow opening GUI in air without clicking a block
  allow-air-click: true

# Menu Provider (NATIVE, ZMENU, DELUXEMENUS, COMMAND)
menu-provider:
  # "NATIVE": Uses built-in sleek GUIs with custom textures & CustomModelData support
  # "COMMAND": Automatically executes a command (e.g. zMenu or DeluxeMenus) on Shift+Click
  type: NATIVE
  command: "zmenu open infinity_pickaxe %player%"

# Leveling Formula & XP Calculation
leveling:
  # XP calculation type: "FORMULA" or "CUSTOM_TABLE"
  type: FORMULA
  formula:
    # Base XP needed for level 1
    base-xp: 100
    # Multiplier exponent: required_xp = base_xp * (level ^ exponent) + (level * linear)
    exponent: 1.35
    linear: 50
  
  # Sounds played upon level up
  levelup-sound:
    enabled: true
    sound: ENTITY_PLAYER_LEVELUP
    volume: 1.0
    pitch: 1.2
  
  # Particles on level up
  levelup-particles:
    enabled: true
    particle: TOTEM_OF_UNDYING
    count: 30

# Anti-Exploit Protection
anti-exploit:
  # Prevent XP from blocks placed by players
  prevent-placed-blocks: true
  # Max blocks to store in memory before oldest cleanup
  placed-blocks-cache-size: 50000
  # Ignore creative mode mining
  ignore-creative: true

duplicate-protection:
  # Heuristic simultaneous-duplicate scans. This does not detect copies that are
  # never observable at the same time (for example, an offline inventory and an unopened chest).
  scan-interval-ticks: 1200
  debounce-ticks: 10
  container-recursion-depth: 3

# Visual Progress Bar Settings (used in Lore and Menus)
progress-bar:
  total-bars: 20
  completed-symbol: "■"
  uncompleted-symbol: "□"
  completed-color: "<#00FF88>"
  uncompleted-color: "<#555555>"

# Pickaxe Visual Lore Template (Fully customizable with MiniMessage & Hex Colors)
gear-lore:
  # Prepended to non-pickaxe profile lore while duplicate protection restricts
  # the item. Emptying this list hides only the warning, not enforcement.
  # Placeholders: %uuid%, %profile%, %material%.
  quarantine-lore:
    - "<red><b>QUARANTINED GEAR</b></red>"
    - "<gray>Duplicate UUID: <white>%uuid%</white></gray>"
    - "<gray>Profile: <white>%profile%</white></gray>"
    - "<yellow>Contact an administrator to resolve this item.</yellow>"

pickaxe-lore:
  # Set to true if you want to override the item name. Default is false (keeps default/vanilla/anvil name).
  custom-display-name: false
  display-name: "<gradient:#00E5FF:#0077FE><b>INFINITY PICKAXE</b></gradient>"
  # Prepended only while duplicate protection restricts this item. Empty the
  # list to hide the visual warning; this never changes quarantine enforcement.
  # Placeholders: %uuid%, %profile%.
  quarantine-lore:
    - "<red><b>QUARANTINED PICKAXE</b></red>"
    - "<gray>Duplicate UUID: <white>%uuid%</white></gray>"
    - "<yellow>Contact an administrator to resolve this item.</yellow>"
  lore:
    - "<dark_gray>---------------------</dark_gray>"
    - "<gray>Level: <gold><b>%level%</b></gold></gray>"
    - "<gray>Progress: %xp_bar%</gray>"
    - "<gray>Mined Blocks: <yellow>%blocks_mined%</yellow></gray>"
